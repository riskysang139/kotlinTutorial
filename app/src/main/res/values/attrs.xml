<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="VtecomCircleIndicator">
        <attr name="vtecom_ci_width" format="dimension" />
        <attr name="vtecom_ci_height" format="dimension" />
        <attr name="vtecom_ci_margin" format="dimension" />
        <attr name="vtecom_ci_animator" format="reference" />
        <attr name="vtecom_ci_animator_reverse" format="reference" />
        <attr name="vtecom_ci_drawable" format="reference" />
        <attr name="vtecom_ci_drawable_unselected" format="reference" />

        <attr name="vtecom_ci_orientation" format="enum">
            <!-- Defines an horizontal widget. -->
            <enum name="horizontal" value="0" />
            <!-- Defines a vertical widget. -->
            <enum name="vertical" value="1" />
        </attr>

        <attr name="vtecom_ci_gravity">
            <!-- Push object to the top of its container, not changing its size. -->
            <flag name="top" value="0x30" />
            <!-- Push object to the bottom of its container, not changing its size. -->
            <flag name="bottom" value="0x50" />
            <!-- Push object to the left of its container, not changing its size. -->
            <flag name="left" value="0x03" />
            <!-- Push object to the right of its container, not changing its size. -->
            <flag name="right" value="0x05" />
            <!-- Place object in the vertical center of its container, not changing its size. -->
            <flag name="center_vertical" value="0x10" />
            <!-- Grow the vertical size of the object if needed so it completely fills its container. -->
            <flag name="fill_vertical" value="0x70" />
            <!-- Place object in the horizontal center of its container, not changing its size. -->
            <flag name="center_horizontal" value="0x01" />
            <!-- Grow the horizontal size of the object if needed so it completely fills its container. -->
            <flag name="fill_horizontal" value="0x07" />
            <!-- Place the object in the center of its container in both the vertical and horizontal axis, not changing its size. -->
            <flag name="center" value="0x11" />
            <!-- Grow the horizontal and vertical size of the object if needed so it completely fills its container. -->
            <flag name="fill" value="0x77" />
            <!-- Additional option that can be set to have the top and/or bottom edges of
            the child clipped to its container's bounds.
            The clip will be based on the vertical gravity: a top gravity will clip the bottom
            edge, a bottom gravity will clip the top edge, and neither will clip both edges. -->
            <flag name="clip_vertical" value="0x80" />
            <!-- Additional option that can be set to have the left and/or right edges of
            the child clipped to its container's bounds.
            The clip will be based on the horizontal gravity: a left gravity will clip the right
            edge, a right gravity will clip the left edge, and neither will clip both edges. -->
            <flag name="clip_horizontal" value="0x08" />
            <!-- Push object to the beginning of its container, not changing its size. -->
            <flag name="start" value="0x00800003" />
            <!-- Push object to the end of its container, not changing its size. -->
            <flag name="end" value="0x00800005" />
        </attr>
    </declare-styleable>
    <declare-styleable name="VtecomCircleImageView">
        <attr name="vtecom_civ_border_width" format="dimension" />
        <attr name="vtecom_civ_border_color" format="color" />
        <attr name="vtecom_civ_border_overlay" format="boolean" />
        <attr name="vtecom_civ_circle_background_color" format="color" />
    </declare-styleable>

    <declare-styleable name="VtecomShopInfoView">
        <attr name="vtecom_si_available_product" format="integer" />
        <attr name="vtecom_si_rating" format="string" />
        <attr name="vtecom_si_order" format="integer" />
        <attr name="vtecom_si_thumbnail" format="string" />
        <attr name="vtecom_si_name" format="string" />
        <attr name="vtecom_si_address" format="string" />
        <attr name="vtecom_si_column" format="integer" />
        <attr name="vtecom_si_startline_color" format="color" />
        <attr name="vtecom_si_endline_color" format="color" />
        <attr name="vtecom_si_name_color" format="color" />
        <attr name="vtecom_si_address_color" format="color" />
        <attr name="vtecom_si_value_av_product_color" format="color" />
        <attr name="vtecom_si_value_rating_color" format="color" />
        <attr name="vtecom_si_value_order_color" format="color" />
        <attr name="vtecom_si_title_av_product_color" format="color" />
        <attr name="vtecom_si_title_rating_color" format="color" />
        <attr name="vtecom_si_title_order_color" format="color" />
        <attr name="vtecom_si_show_view_shop_button">
            <flag name="show" value="1" />
            <flag name="hide" value="0" />
        </attr>
    </declare-styleable>

    <declare-styleable name="VtecomSearchActionBarView">
        <attr name="vtecom_sb_background" format="reference" />
        <attr name="vtecom_sb_search_icon" format="reference" />
        <attr name="vtecom_sb_clear_icon" format="reference" />
        <attr name="vtecom_sb_icon_color" format="color" />
        <attr name="vtecom_sb_text_color" format="color" />
        <attr name="vtecom_sb_hint_text_color" format="color" />
    </declare-styleable>

    <declare-styleable name="ExpandableTextView">
        <attr name="trimLength" format="integer" />
    </declare-styleable>

</resources>